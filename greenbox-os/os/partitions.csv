# greenbox OS, 4MB flash.
#
# No factory partition on purpose: with only OTA slots present the bootloader
# falls back to ota_0 when otadata is blank, which is what a fresh flash gives.
# ota_1 is reserved for rollback-safe OS updates once WiFi lands.
#
# gslot is data, not app - an app-type partition would tempt the bootloader to
# boot it. It is reserved for the future execute-in-place loader and unused in
# v1, where guests are copied into RAM instead.
#
# Name,     Type, SubType,  Offset,   Size
nvs,        data, nvs,      0x9000,   0x4000
otadata,    data, ota,      0xd000,   0x2000
phy_init,   data, phy,      0xf000,   0x1000
ota_0,      app,  ota_0,    0x10000,  0x100000
ota_1,      app,  ota_1,    0x110000, 0x100000
gslot,      data, 0x40,     0x210000, 0x80000
storage,    data, spiffs,   0x290000, 0x170000
